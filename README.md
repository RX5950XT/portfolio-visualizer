# 📈 Portfolio Visualizer (投資組合視覺化管理工具)

一個現代化、輕量級且無伺服器負擔的個人投資組合管理應用程式。專為長期投資者設計，提供資產配置視覺化、即時股價追蹤與多幣別損益分析。

---

## ✨ 核心功能 (Features)

### 💼 資產管理
- **多投資組合 (Multi-Portfolio)**：支援建立多個投資組合，可將「長期退休帳戶」與「短期投機帳戶」分開管理。
- **現金部位追蹤**：獨立記錄現金餘額，提供更真實的總資產視圖。
- **完整 CRUD**：支援持股的新增、編輯、刪除，並能記錄詳細的買入日期與成本。

### 📊 視覺化分析
- **資產配置圓餅圖**：自動計算各持股佔比，並按權重自動排序。
- **30 日損益走勢**：透過長條圖清晰呈現近期的資產盈虧變化。
- **歷史資產曲線**：追蹤總資產隨時間的成長軌跡 (Area Chart)。
- **ETF 費用率分析**：自動抓取並計算投資組合的「加權平均費用率」，助您控管隱形成本。

### 🌍 全球市場支援
- **台美股通吃**：
  - 美股：支援所有 US Stock & ETF (如 VOO, NVDA, AAPL)。
  - 台股：支援上市 (`2330.TW`) 與上櫃 (`6547.TWO`) 股票。
- **統一貨幣**：自動將美格資產透過即時匯率換算為 **新台幣 (TWD)** 顯示，資產總值一目了然。

### 🛡️ 安全與權限 (RBAC)
- **雙重權限機制**：
  - 🔑 **Admin (管理員)**：完整讀寫權限，可執行交易。
  - 👁️ **Guest (訪客)**：唯讀模式，適合分享投資組合給親友或社群查看。
- **資料安全**：敏感操作 (POST/DELETE) 強制後端驗證，資料庫層級保護。

---

## 🛠️ 技術堆疊 (Tech Stack)

- **Frontend Framework**: [Next.js 16](https://nextjs.org/) (App Router)
- **Database**: [Supabase](https://supabase.com/) (PostgreSQL)
- **Styling**: [Tailwind CSS v4](https://tailwindcss.com/) + [shadcn/ui](https://ui.shadcn.com/)
- **Charts**: [Recharts](https://recharts.org/)
- **Icons**: [Lucide React](https://lucide.dev/)
- **Data Source**: Yahoo Finance (via HTTP Proxy)

---

## 🚀 快速開始 (Getting Started)

### 1. 複製專案
```bash
git clone https://github.com/your-username/portfolio-visualizer.git
cd portfolio-app
```

### 2. 安裝套件
```bash
npm install
```

### 3. 設定環境變數
在專案根目錄 (`portfolio-app/`) 建立 `.env.local` 檔案：

```env
# Supabase 連線資訊 (從 Supabase Dashboard 取得)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 應用程式密碼 (請自行設定強密碼)
SITE_PASSWORD=admin_secret_password
GUEST_PASSWORD=guest_view_password
```

### 4. 初始化資料庫
前往 Supabase SQL Editor，執行 `migrations/` 資料夾中的 SQL 腳本，依序建立：
1. `schema.sql` (基礎表格)
2. `create_portfolios.sql` (投資組合支援)
3. `create_transactions.sql` (交易紀錄支援)

### 5. 啟動開發伺服器
```bash
npm run dev
```
打開瀏覽器訪問 [http://localhost:3000](http://localhost:3000)。

---

## 📂 專案結構

```
portfolio-app/
├── app/
│   ├── api/                # Backend API Routes (Auth, Data Proxy)
│   ├── dashboard/          # 主控台頁面
│   ├── holdings/           # 持股管理
│   └── transactions/       # 交易紀錄
├── components/
│   ├── charts/             # 視覺化圖表組件
│   ├── holdings/           # 持股列表與表單
│   └── ui/                 # 共用 UI 元件
├── lib/
│   ├── auth.ts             # 權限驗證邏輯
│   └── supabase.ts         # 資料庫連線實例
└── migrations/             # 資料庫 Schema 定義檔
```


